# Name,   Type, SubType, Offset,   Size, Flags
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  2M,
# Keep the existing NVS, PHY, and app offsets unchanged. This key partition is
# unused in normal dev builds; it becomes the encrypted NVS key store only when
# the separately approved flash-encryption profile is enabled.
nvs_keys, data, nvs_keys,0x210000, 0x1000, encrypted
